When the user gives the first prompt of the session, then firecrawl is used to find the most popular competitor in that category. Then, using firecrawl instead of following a certain template, firecrawl studies whatever pages are there, which sections are in which pages, and those are the onne that will be created insyead of following a template. Now instead of coding the whole thing at once, it is coded section by section and using firecrawler nexa. It should be able to take peek detailing from that competitor website. For example the button color, size of each section of the section that is building, background color, and background etc. And it should be able to extract extractable assets and components like images etc. using firecrawler and EXA possibly. If the background is something that is really complex or an image, then an image should be used by it. If it is something that is too detailed for a stock image, then AI should be able to use our image AI agent, which is the one which will clone that background using our image generation model. If the background contains an animation that is too detailed to be coded, then it should use the video generation model to add that same animation. It's not only about background animation but also other animations that are too complex to generate and are not on any icon or something . Now as it is coding a section, it should have all the same font style, etc., same colors and all. Once a section is coded, a critical loop is followed, seeing the closeness to which it has been cloned. It should have a very high score in order to proceed to a next section. If not, then make changes to that particular section. Each section is generated in this way only and our agent should be smart enough to ask questions if it has any doubt. What, the repo that we imported, was a white coding application or a genetic Vibe coding application so these instructions are for my Vibe coding application.  For the image generation model it should be able to use Gemini 3.1 Flash image ( Nano Banana 2 ) from Together AI. For the video generation model it should use Sora 2 from OpenAI. If the agent needs to fetch results from the web, it should be able to use firecrawler and exa based on their use case. Don't care about copyright for now. I'll take care of that later.